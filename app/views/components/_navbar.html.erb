<div class="navbar navbar-expand-sm navbar-light navbar-lewagon fixed-top">
  <%= link_to lists_path(@lists), class: "navbar-brand" do %>
    <%= image_tag asset_url('logon.png') %> Movie List
  <% end %>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>


  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
      <li class="nav-item">
        <%= link_to "Search Movie", movies_path(@movies), class: "nav-link mr-3 btn btn-outline-dark"%>
      </li>
      <li class="nav-item">
        <%= link_to "New lists", new_list_path(@list), class: "nav-link mr-1 btn btn-outline-dark"%>
      </li>

      <li class="nav-item dropdown">
         <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
          <%= link_to "Home", lists_path(@lists), class: "dropdown-item" %>
          <%= link_to "New List", new_list_path(@lists), class: "dropdown-item" %>
          <%= link_to "All lists", lists_path(@lists), class: "dropdown-item" %>
          <%= link_to "Search Movie", movies_path(@movies), class: "dropdown-item" %>
        </div>
      </li>
    </ul>
  </div>
</div>