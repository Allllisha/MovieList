<div class = "home-banner">
<%= image_tag 'move-save.png', :size => '600x400', class: "neon blink" %>
<p><span id="banner-typed-text"></span>
</p>
</div>


<div class="list-container">
<h1>All list 🎬</h1>
</div>

<div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel" data-interval="3000">
  <ol class="carousel-indicators">
    <li data-target="#carouselExampleIndicators" data-slide-to="0" class="active"></li>
    <% @lists.drop(1).count.times do |index| %>
      <li data-target="#carouselExampleIndicators" data-slide-to="#{index}"></li>
    <% end %>
  </ol>

  <div class="carousel-inner">
    <div class="carousel-item active text-white">
      <% @lists.first(1).each do |list| %>
         <%= image_tag list.photo, :width => '100%', :height => '400px', class: "carousel_image" %>
         <div class="card-img-overlay">
    <h3 class="card-title"><%= link_to list.name, list_path(list), class: "white-text" %></h3>
    <%= link_to edit_list_path(list) do%>
      <i class="far fa-edit"></i>
      <% end %>
    <%= link_to list_path(list), method: :delete do%>
    <i class="fas fa-trash-alt"></i>
    <% end %>
  </div>
      <% end %>
    </div> 
    <% @lists.drop(1).each do |list| %>
      <div class="carousel-item text-white">
    <%= image_tag list.photo, :width => '100%', :height => '400px', class: "carousel_image" %>
    <div class="card-img-overlay">
    <h3 class="card-title"><%= link_to list.name, list_path(list), class: "white-text" %></h3>
    <p class="item">
    <%= link_to edit_list_path(list) do%>
      <i class="far fa-edit"></i>
      <% end %>
    </p>
    <p>
    <%= link_to list_path(list), method: :delete do%>
    <i class="fas fa-trash-alt"></i>
    <% end %>
    </p>
  </div>
      </div>
    <% end %>
  </div>
  <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
  </a>
  <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="sr-only">Next</span>
  </a>
</div>


 
<div class="list-container">
<h1>My list 🎬</h1>
</div>


<div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel" data-interval="3000">
  <ol class="carousel-indicators">
    <li data-target="#carouselExampleIndicators" data-slide-to="0" class="active"></li>
    <% @favlists.drop(1).count.times do |index| %>
      <li data-target="#carouselExampleIndicators" data-slide-to="#{index}"></li>
    <% end %>
  </ol>

  <div class="carousel-inner">
    <div class="carousel-item active text-white">
      <% @favlists.first(1).each do |favlist| %>
         <%= image_tag favlist.photo, :width => '100%', :height => '400px', class: "carousel_image" %>
         <div class="card-img-overlay">
    <h3 class="card-title"><%= link_to favlist.name, list_path(favlist), class: "white-text" %></h3>
    <p class="item">
    <%= link_to edit_list_path(favlist) do%>
      <i class="far fa-edit"></i>
      <% end %>
    </p>
    <p>
    <%= link_to list_path(favlist), method: :delete do%>
    <i class="fas fa-trash-alt"></i>
    <% end %>
    </p>
  </div>
      <% end %>
    </div> 
    <% @favlists.drop(1).each do |favlist| %>
      <div class="carousel-item text-white">
    <%= image_tag favlist.photo, :width => '100%', :height => '400px', class: "carousel_image" %>
    <div class="card-img-overlay">
    <h3 class="card-title"><%= link_to favlist.name, list_path(favlist), class: "white-text" %></h3>
    <p class="item">
    <%= link_to edit_list_path(favlist) do%>
      <i class="far fa-edit"></i>
      <% end %>
    </p>
    <p>
    <%= link_to list_path(favlist), method: :delete do%>
    <i class="fas fa-trash-alt"></i>
    <% end %>
    </p>
  </div>
      </div>
    <% end %>
  </div>
  <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
  </a>
  <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="sr-only">Next</span>
  </a>
</div>



 

 
 
   