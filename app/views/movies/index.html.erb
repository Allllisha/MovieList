<%# <div class="movie-banner"> %>
<%# <h2>Cinema</h2>
<%= form_tag movies_path, method: :get do %>
  <%# <div class ="search-tag">
  <%# <%= text_field_tag :query,params[:query], class: "form-control", placeholder: "Find a movie"%>
  <%# <% end %>
  <%# </div>
<%# </div> %>

  <div class="list-container">
    <h1>Movies 🎬</h1>
  </div>
  <div class="movie-wrapper d-flex justify-content-around my-box-light flex-wrap mb-2">
    <% @movies.each do |movie| %>
      <div class="container-glass">
        <%= image_tag movie.poster_url, :width => '300px', :height => '200px', alt: "movie poster", class: "img" %>
        <p class="movie-card-text">
          <div class="d-flex">
            <h4><i class="fas fa-star"></i></h4>
            <h4><%= movie.rating %></h4>
            <h5>/10</h5>
          </div>
          <h6><%= movie.title %></h6>
          <button class="btn"><%= link_to "Discover", movie_path(movie), class: "white-text" %></button>
        </p>
      </div>
   
    <% end %>
  </div>
<div class="page-navigation">
<%= will_paginate @movies, :previous_label => '&lt', :next_label => '&gt', renderer: WillPaginate::ActionView::BootstrapLinkRenderer %>
</div>